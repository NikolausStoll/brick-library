# AGENT GUIDELINES

- Help the user iterate on the Brick Library tracker described in the workspace README.
- Keep frontend in `frontend/` (Vue 3 + Vite) and backend in `backend/` (Node + Express).
- Preserve the schema and naming conventions laid out in the user's brief for the `sets` table.
- Avoid unnecessary features until the user specifically asks for expansions.
- Reference `brick-library/` for Home Assistant addon metadata when appropriate.
